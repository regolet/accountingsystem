# NextAuth Configuration (Required)
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=your-nextauth-secret-here

# Database Configuration (Required)
DATABASE_URL=postgresql://username:password@localhost:5432/accounting_db
DIRECT_URL=postgresql://username:password@localhost:5432/accounting_db

# SMTP Configuration (Optional - now stored in database)
# These are no longer needed as email settings are managed through the admin interface
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587

# FOR DEPLOYMENT:
# Set these environment variables in your deployment dashboard:
# NEXTAUTH_URL=https://your-app-url.com
# NEXTAUTH_SECRET=your-nextauth-secret-here
# DATABASE_URL=your-supabase-database-url
# DIRECT_URL=your-supabase-database-url